[project]
name = "gradient-ai-backend"
version = "0.1.0"
description = "FastAPI backend for Gradient AI"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

